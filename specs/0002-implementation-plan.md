# 0002 实现计划

## 1. 文档信息

- 文档编号：`0002`
- 文档名称：视频字幕提取与翻译软件实现计划
- 上游规格：`specs/0001-spec.md`
- 目标：将 `0001` 中已确定的产品与技术约束拆解为可执行的开发计划

## 2. 实现目标

首版实现以下闭环能力：

- 基于 `Rust + Tauri 2` 的桌面应用框架
- 导入单个/多个视频文件并形成任务列表
- 选择输出目录，默认输出到视频同目录
- Whisper 模型下载、管理、模型目录落盘
- 音频抽取、转写、生成 `srt`
- 可关闭翻译，或使用 OpenAI 兼容 LLM 进行翻译
- 三种字幕生成模式
- 任务开始、软暂停、删除、清空
- GPU/CPU 检测、Whisper GPU 开关、推荐模型档位
- 术语表、翻译批次、翻译失败回退
- 严格便携模式的数据/配置/密钥存储

## 3. 总体实施策略

### 3.1 开发原则

- 先打通单任务完整链路，再扩展为批量与并发
- 先保证 `关闭翻译 + LLM 翻译` 两条正式路径稳定，再考虑实验性翻译引擎
- 先实现任务执行可靠性，再补性能优化
- 先统一数据模型与任务快照机制，再做 UI 联动
- 先交付最小可用桌面版，再补高级体验

### 3.2 范围边界

本计划只覆盖 `0001` 已确认的首版正式范围。

不纳入本计划主线：

- Google Web 免费模式的稳定交付
- 字幕编辑器
- 多字幕格式
- 自动恢复未完成队列
- 多 Whisper 后端并行支持

## 4. 建议架构

### 4.1 技术栈

- 桌面容器：`Tauri 2`
- 核心语言：`Rust`
- 前端：建议 `React + TypeScript`
- 异步运行时：`tokio`
- 序列化：`serde`
- 配置格式：`json`
- 任务状态持久化：首版可用 `json`，后续可迁移 `sqlite`
- 音频抽取：`ffmpeg`
- HTTP：`reqwest`

### 4.2 分层架构

建议分为以下层次：

1. `UI 层`
   - 主界面
   - 配置页
   - 模型管理页/区域
   - 任务详情/错误信息展示
2. `应用服务层`
   - 任务编排
   - 配置快照生成
   - 输出路径决策
   - 翻译管线调度
3. `领域层`
   - 任务模型
   - 配置模型
   - 字幕模型
   - 术语表规则
4. `基础设施层`
   - 文件系统
   - 加密存储
   - `ffmpeg`
   - Whisper 适配器
   - LLM 适配器
   - 硬件检测

### 4.3 推荐目录结构

```text
src-tauri/
  src/
    app/
      commands/
      services/
      state/
    domain/
      config/
      task/
      subtitle/
      glossary/
      hardware/
    infra/
      ffmpeg/
      whisper/
      translator/
      storage/
      security/
      system/
    main.rs
src/
  app/
    routes/
    pages/
    components/
    hooks/
    stores/
    types/
    services/
```

## 5. 核心模块拆分

### 5.1 前端模块

#### A. 应用框架模块

- 路由与布局
- 全局状态管理
- Tauri 命令调用封装
- 错误提示和通知

#### B. 主界面模块

- 顶部工具栏
- 任务表格
- 行操作按钮
- 进度显示
- 错误查看入口

#### C. 配置页模块

- 翻译引擎配置
- Whisper 配置
- 翻译策略配置
- 字幕生成配置
- 性能与运行配置

#### D. 模型管理模块

- 模型列表
- 下载状态
- 已下载模型展示
- GPU 信息与推荐档位

#### E. 术语表模块

- 列表展示
- 新增/编辑/删除
- 导入导出

### 5.2 Rust 后端模块

#### A. 配置模块

- 加载 `app-config.json`
- 加载/保存 `secrets.enc.json`
- 配置校验
- 配置默认值
- 配置快照生成

#### B. 任务模块

- 任务创建
- 任务状态流转
- 任务队列
- 任务取消/软暂停
- 任务持久化

#### C. 存储模块

- 软件目录初始化
- `models/`
- `config/`
- `data/`
- `logs/`
- `temp/`

#### D. 加密模块

- `API Key` 加密
- `API Key` 解密
- 密钥文件读写
- 启动时兼容空密钥和坏数据

#### E. 硬件检测模块

- CPU 核心数
- 可用内存
- GPU 检测
- 显存获取
- Whisper 是否可用 GPU 后端
- 推荐模型档位

#### F. 模型下载模块

- 镜像地址处理
- 下载任务
- 下载进度
- 校验模型文件
- 删除本地模型

#### G. `ffmpeg` 模块

- 可执行文件检查
- 音频抽取命令
- 错误包装

#### H. Whisper 适配模块

- 模型定位
- 推理设备选择
- 识别语言参数
- 转写结果解析
- 段落/时间戳标准化

#### I. 翻译适配模块

- OpenAI 兼容接口调用
- 连接测试
- 统一翻译接口抽象
- 批量翻译
- 错位检测
- 重试与回退

#### J. 字幕导出模块

- 原文字幕导出
- 翻译字幕导出
- 双语字幕导出
- 文件名规则
- 统一输出目录短哈希规则
- 覆盖写入

## 6. 数据模型与状态设计

### 6.1 配置模型

至少包含：

- `translator.engine`
- `llm.base_url`
- `llm.model`
- `whisper.model`
- `whisper.use_gpu`
- `whisper.recognition_lang`
- `translate.source_lang`
- `translate.target_lang`
- `translate.style`
- `subtitle.mode`
- `subtitle.output_dir_mode`
- `subtitle.custom_output_dir`
- `runtime.max_parallel_tasks`
- `runtime.task_auto_retry_max`

### 6.2 任务模型

至少包含：

- `id`
- `video_path`
- `file_name`
- `status`
- `progress`
- `phase`
- `translator_engine`
- `config_snapshot_id`
- `config_snapshot_summary`
- `output_paths`
- `error_message`
- `created_at`
- `updated_at`

### 6.3 状态流转

建议任务状态流转如下：

`待开始 -> 排队中 -> 提取音频中 -> 识别中 -> 翻译中 -> 导出中 -> 已完成`

异常分支：

- 任意阶段 -> `暂停请求中` -> `已暂停`
- 任意阶段 -> `失败`
- `失败` -> `开始` -> 重新执行
- `已暂停` -> `开始` -> 继续执行

### 6.4 配置快照策略

任务在点击开始时生成快照，并写入任务记录。

快照必须至少覆盖：

- 翻译引擎
- 字幕模式
- Whisper 模型
- 是否使用 GPU
- 识别语言
- 翻译源语言/目标语言
- 输出目录
- 输出覆盖规则
- 术语表版本
- 翻译风格
- 最大重试次数

## 7. 分阶段实现计划

### 阶段 0：项目初始化

目标：

- 初始化 Tauri 2 项目结构
- 建立前后端通信约定
- 建立基础目录和配置文件结构

任务：

- 创建 Tauri 2 + 前端项目骨架
- 建立 Rust 命令接口规范
- 创建软件目录初始化逻辑
- 创建基础日志方案
- 创建配置文件默认模板
- 创建加密密钥文件模板

产出：

- 可启动桌面空壳应用
- 首次启动自动创建软件目录结构
- 前后端最小通信跑通

### 阶段 1：配置与存储基础设施

目标：

- 打通配置读写、加密密钥存储、目录落盘

任务：

- 实现 `app-config.json` 读写
- 实现 `secrets.enc.json` 加密读写
- 实现配置校验与默认值填充
- 实现配置页基础 UI
- 实现 LLM 连接测试接口

产出：

- 用户可以保存和读取配置
- `API Key` 不以明文落盘
- 连接测试可返回结构化结果

### 阶段 2：任务列表与主界面

目标：

- 完成主界面工具栏和任务表格

任务：

- 实现输出目录选择
- 实现导入单个/多个视频
- 实现任务列表渲染
- 实现行操作按钮
- 实现混合任务时第三列逻辑
- 实现任务持久化缓存

产出：

- 用户可以导入视频并看到任务
- 输出目录规则可用
- 第三列显示规则符合规格

### 阶段 3：硬件检测与模型管理

目标：

- 完成 GPU/CPU 检测和 Whisper 模型管理

任务：

- 实现 CPU、内存、GPU、显存检测
- 实现 GPU 可用性判断
- 实现推荐模型档位规则
- 实现模型下载/删除/已下载展示
- 实现镜像地址配置和默认值

产出：

- 用户可以查看 GPU 信息
- 用户可以下载和管理 Whisper 模型
- 能根据硬件给出推荐模型

### 阶段 4：单任务转写闭环

目标：

- 完成从视频到原文字幕的完整链路

任务：

- 实现 `ffmpeg` 检查
- 实现音频抽取
- 实现 Whisper 调用
- 实现时间戳标准化
- 实现原文 `srt` 导出
- 实现覆盖写入逻辑

产出：

- 在关闭翻译模式下可完整生成原文字幕
- 同一个视频重复执行时覆盖旧结果

### 阶段 5：LLM 翻译闭环

目标：

- 完成稳定的 OpenAI 兼容翻译链路

任务：

- 实现翻译适配接口
- 实现分批翻译和上下文窗口
- 实现术语表注入
- 实现错位检测
- 实现重试和逐条回退
- 实现翻译结果缓存与展示

产出：

- 可生成翻译字幕
- 可生成双语字幕
- 翻译错位时不会破坏时间轴

### 阶段 6：批量任务与调度

目标：

- 支持多任务并发与稳定调度

任务：

- 实现任务队列
- 实现最大任务并发
- 实现翻译请求并发限制
- 实现自动并发策略
- 实现软暂停
- 实现删除中的安全取消

产出：

- 批量任务可稳定运行
- UI 不因后台任务阻塞
- 失败任务不影响其他任务

### 阶段 7：打磨与验收

目标：

- 对齐规格和验收标准

任务：

- 补齐错误提示
- 补齐日志与诊断信息
- 补齐空状态和边界交互
- 验证覆盖写入、短哈希、配置快照等关键规则
- 进行回归测试

产出：

- 可用于首版交付的候选版本

## 8. 详细任务清单

### 8.1 Rust 后端任务

#### 配置与存储

- 定义配置结构体与默认值
- 实现配置迁移逻辑
- 实现敏感信息加密读写
- 实现目录初始化
- 实现配置快照结构体

#### 命令接口

- `get_app_info`
- `get_config`
- `save_config`
- `test_llm_connection`
- `get_hardware_info`
- `list_models`
- `download_model`
- `delete_model`
- `import_videos`
- `list_tasks`
- `start_tasks`
- `pause_task`
- `delete_task`
- `clear_tasks`
- `open_output_dir`

#### 任务执行

- 建立任务执行器
- 建立阶段事件上报
- 建立进度事件上报
- 建立取消令牌
- 建立软暂停标记

#### 音视频与字幕

- `ffmpeg` 检查与错误码包装
- 音频抽取参数固定化
- Whisper 输出转内部字幕结构
- `srt` 编码器
- 双语字幕合并器

#### 翻译

- 构建翻译 prompt
- 构建术语表提示片段
- 实现批量翻译请求
- 实现响应解析器
- 实现错位检测器
- 实现回退执行器

### 8.2 前端任务

#### 主界面

- 工具栏组件
- 输出目录选择器
- 文件导入按钮
- 清除按钮与确认逻辑
- 开始任务按钮
- 任务表格
- 行操作按钮

#### 配置页

- 翻译引擎切换联动
- LLM 参数表单
- Whisper 参数表单
- GPU 开关联动
- 字幕模式表单
- 保存与校验

#### 模型管理

- 硬件信息卡片
- 推荐模型卡片
- 下载进度 UI
- 已下载模型列表

#### 术语表

- 表格视图
- 编辑弹窗
- 导入导出按钮

#### 状态同步

- 任务列表 store
- 配置 store
- 事件订阅
- 错误通知

## 9. 测试计划

### 9.1 单元测试

重点覆盖：

- 配置校验
- 加密/解密
- 输出文件命名
- 统一输出目录短哈希
- 翻译错位检测
- 字幕合并逻辑
- 术语表匹配规则

### 9.2 集成测试

重点覆盖：

- 首次启动初始化目录
- 保存配置并重启后读取
- 模型下载到软件目录
- `ffmpeg` 缺失时提示
- 单视频转写闭环
- 单视频翻译闭环
- 双语字幕导出

### 9.3 端到端测试

场景至少包括：

- 仅原字幕模式
- 原文+翻译双文件模式
- 单文件双语模式
- 批量导入多个视频
- 统一输出目录同名文件冲突
- 同一视频重复执行覆盖
- 任务运行中暂停
- 任务运行中删除
- 修改全局配置后旧任务不受影响

### 9.4 人工验收测试

按 `0001` 中的验收标准逐条对照。

建议制作验收清单：

- 配置与密钥
- 模型下载与硬件识别
- 任务表格和列显示
- 输出文件命名与覆盖
- 翻译质量与回退
- 失败任务隔离

## 10. 风险与应对

### 10.1 Whisper 后端集成风险

风险：

- GPU 支持与推理库兼容性不稳定

应对：

- 首版仅选一个实际可落地的 Whisper 后端
- CPU 路径必须可用
- GPU 不可用时自动降级或明确提示

### 10.2 LLM 接口兼容风险

风险：

- 各家 OpenAI 兼容实现细节不同

应对：

- 统一抽象请求结构
- 保持适配层隔离
- 连接测试返回清晰错误

### 10.3 批量翻译错位风险

风险：

- LLM 返回段落不一致导致字幕错位

应对：

- 严格编号校验
- 失败重试
- 自动缩批
- 最终逐条回退

### 10.4 便携模式安全风险

风险：

- 密钥虽然加密，但仍和软件放在同目录

应对：

- 明确 UI 提示边界
- 不记录明文 Key
- 加密文件和配置文件分离

### 10.5 Windows 目录权限风险

风险：

- 用户将软件放到不可写目录

应对：

- 启动时检测软件目录写权限
- 无权限时阻止关键流程并明确提示

## 11. 建议排期

若由单人主开发推进，建议按以下顺序：

### 第 1 周

- 阶段 0
- 阶段 1
- 阶段 2 的基础 UI

### 第 2 周

- 阶段 3
- 阶段 4

### 第 3 周

- 阶段 5
- 阶段 6

### 第 4 周

- 阶段 7
- 回归测试
- 打包与首版验收

## 12. 交付定义

满足以下条件视为本实现计划完成：

- `0001` 首版正式范围全部有对应实现
- `0001` 验收标准全部可被验证
- `0002` 中列出的核心模块均有落地代码
- 有最小测试集覆盖关键规则
- 有可运行的桌面应用构建产物

## 13. 后续文档建议

在 `0002` 之后建议继续补以下文档：

- `0003-architecture.md`
- `0004-data-models.md`
- `0005-task-breakdown.md`
- `0006-test-plan.md`

## 14. 增量实现计划：翻译引擎扩展与原字幕智能分段

本节对应 `0001` 新增设计，重点补足两块能力：

- `翻译引擎` 明确支持 `关闭 / LLM / Google Web 免费模式（实验性）`
- `原字幕分段策略` 明确支持 `关闭 / 自动 / 规则优先 / LLM 优先`

实现原则：

- 翻译和原字幕分段必须解耦，不能共用一个开关
- 原字幕分段发生在 `Whisper 转写之后、翻译之前`
- `Google Web 免费模式（实验性）` 只负责翻译，不参与原字幕分段
- `自动` 分段模式在无可用 LLM 配置时必须自动回退到规则分段
- 首版原字幕分段目标是“观感基本对齐”，不是独立 `forced alignment`

### 14.1 增量目标

本轮新增实现目标：

- 用户可独立配置翻译引擎，不影响原字幕分段策略
- 用户可在不启用翻译时，单独对原字幕做智能分段
- 系统可根据配置在 `LLM 分段` 和 `规则分段` 之间自动切换或回退
- 原字幕分段后的时间轴优先使用 `Whisper` 词级时间；无词级时间时退化为近似分配
- 翻译阶段以“分段后的原字幕”作为输入，而不是以 Whisper 原始片段直接翻译
- 任务快照需固化 `分段策略 + 时间策略 + 阈值`

### 14.2 配置模型增量

需在配置模型中新增独立分段配置组，例如：

```json
{
  "segmentation": {
    "strategy": "auto",
    "max_chars_per_segment": 42,
    "max_duration_seconds": 6.0,
    "timing_mode": "word_timestamps_first"
  }
}
```

新增字段说明：

- `segmentation.strategy`
  - `disabled`
  - `auto`
  - `rules_only`
  - `llm_preferred`
- `segmentation.max_chars_per_segment`
  - 单条原字幕最大字符数阈值
- `segmentation.max_duration_seconds`
  - 单条原字幕最大持续时长阈值
- `segmentation.timing_mode`
  - `word_timestamps_first`
  - `approximate_reflow`

现有配置联动调整：

- `translator.engine`
  - `off`
  - `llm`
  - `google_web`
- 当 `translator.engine = off` 时，翻译专属 UI 隐藏，但 `segmentation.*` 仍可编辑
- 当 `segmentation.strategy = auto` 且 `LLM` 不可用时，保存配置不报错；执行期自动回退
- 当 `segmentation.strategy = llm_preferred` 且 `LLM` 不可用时，开始任务前需给出明确提示

### 14.3 任务快照与状态机增量

任务快照需新增以下字段：

- `snapshot_segmentation_strategy`
- `snapshot_segmentation_max_chars`
- `snapshot_segmentation_max_duration_seconds`
- `snapshot_segmentation_timing_mode`
- `snapshot_translation_engine`
- `snapshot_translation_source_lang`
- `snapshot_translation_target_lang`

任务执行阶段扩展为：

`待开始 -> 排队中 -> 提取音频中 -> 识别中 -> 原字幕分段中 -> 翻译中 -> 导出中 -> 已完成`

状态流转规则：

- `原字幕分段中` 仅在 `segmentation.strategy != disabled` 时进入
- 若分段回退为规则分段，不应改变任务主流程，只在任务详情中记录说明
- 若分段结果非法，则必须继续回退，不得把错误分段直接传给翻译或导出
- `翻译中` 永远基于“已确定的原字幕片段”执行

### 14.4 后端模块增量拆分

需新增或扩展以下 Rust 模块职责。

#### A. 字幕分段领域模块

- 定义原字幕分段输入输出模型
- 表达“原始片段 / 分段后片段 / 时间来源”
- 提供合法性校验：
  - 分段前后文本总量一致
  - 片段顺序一致
  - 时间单调递增

建议新增结构：

- `RecognizedCue`
- `SegmentedCue`
- `SegmentationPlan`
- `SegmentationTimingMode`
- `SegmentationFallbackReason`

#### B. 规则分段器

- 按标点切分
- 按最大字符数切分
- 按最大持续时长切分
- 处理无标点长文本
- 处理过短片段回并

实现要求：

- 不允许改写原文字符内容
- 优先切超长片段，不强制全量重切
- 若规则切分后出现极短片段，自动与前后片段合并

#### C. LLM 分段器

- 使用统一 LLM 适配层发送“断句建议”请求
- 输入为原字幕文本和约束条件，不直接请求“改写文本”
- 输出必须是：
  - 断句位置
  - 或保持原文不变的新片段数组

实现要求：

- 必须校验 LLM 返回文本与原文是否一致
- 若不一致：
  - 先重试
  - 再回退为规则分段
- `Google Web 免费模式（实验性）` 不复用此模块

#### D. 分段时间回填模块

- `词级时间优先` 路径：
  - 读取 Whisper 词级或更细粒度时间
  - 以分段边界映射开始/结束时间
- `近似对齐` 路径：
  - 按字符比例分配
  - 标点/停顿加权
  - 最小时长修正
  - 首尾时间夹紧

实现要求：

- 不允许时间重叠
- 不允许新片段超出原始片段时间范围
- 若词级时间缺失，必须记录降级原因

#### E. 翻译适配层扩展

在现有统一翻译接口中明确拆成两个实现：

- `LlmTranslator`
- `GoogleWebTranslator`

约束：

- 两者都只接收“已经分段完成的原字幕”
- 翻译输出不得修改时间轴
- 双语模式以上行译文、下行原文合并
- Google 适配器必须和 LLM 参数表单解耦

### 14.5 前端配置页增量

配置页需新增或调整以下交互：

#### A. 翻译引擎区

- 引擎下拉：
  - `关闭`
  - `LLM`
  - `Google Web 免费模式（实验性）`
- `LLM` 时显示：
  - `Base URL`
  - `API Key`
  - `模型名称`
  - `连接测试`
- `Google Web 免费模式（实验性）` 时显示：
  - 可选服务地址
  - 可选代理/QPS 配置
  - 实验性说明

#### B. 原字幕分段区

- `原字幕分段策略` 下拉：
  - `关闭`
  - `自动`
  - `规则优先`
  - `LLM 优先`
- `分段时间策略` 下拉：
  - `词级时间优先`
  - `近似对齐`
- `单条字幕最大字符数`
- `单条字幕最大持续时长`

交互规则：

- 当 `翻译引擎 = 关闭` 时，该分段区仍可见
- 当 `原字幕分段策略 = 自动` 时，显示辅助说明：
  - “已配置 LLM 时优先使用 LLM，否则自动回退为规则分段”
- 当 `原字幕分段策略 = LLM 优先` 时，若当前缺 LLM 配置，表单层提示风险，但不必阻止保存
- 若未来增加任务详情页，应显示：
  - 本次分段方式
  - 是否发生规则回退
  - 是否使用近似时间分配

### 14.6 后端执行流水线调整

新增的单任务执行顺序建议如下：

1. 抽取音频
2. 调用 `whisper.cpp` 生成原始字幕
3. 标准化为内部 `cue` 结构
4. 根据 `segmentation.strategy` 决定是否执行原字幕分段
5. 根据 `segmentation.timing_mode` 生成分段后的时间轴
6. 写入原字幕缓存
7. 根据 `translator.engine + subtitle.mode` 决定是否执行翻译
8. 翻译输出挂载到分段后的原字幕结构
9. 导出原文 / 翻译 / 双语字幕

执行决策表：

- `segmentation = disabled`
  - 直接沿用 Whisper 原始片段
- `segmentation = rules_only`
  - 规则分段
- `segmentation = auto`
  - 有 LLM 配置：LLM 分段 -> 校验 -> 失败则规则回退
  - 无 LLM 配置：直接规则分段
- `segmentation = llm_preferred`
  - 有 LLM 配置：LLM 分段 -> 校验 -> 失败则规则回退，并标记回退
  - 无 LLM 配置：任务启动前提示并允许用户取消或改用规则分段

### 14.7 详细开发任务清单

#### 后端

- 扩展配置模型、默认值、JSON 序列化结构
- 扩展任务快照模型与摘要展示
- 新增字幕分段领域对象和校验器
- 实现规则分段器
- 实现 LLM 分段器
- 实现分段结果文本一致性校验
- 实现分段后时间回填器
- 在 `Whisper -> 分段 -> 翻译 -> 导出` 主链路接入分段步骤
- 扩展统一翻译适配接口，区分 `LLM` 与 `Google`
- 增加分段回退日志和任务详情字段

#### 前端

- 配置页新增“原字幕分段策略”分组或字段
- 配置页新增分段阈值与时间策略表单
- 翻译引擎切换时调整联动显示
- 主界面/任务详情增加“分段回退”状态摘要
- 在保存配置前做基础前端校验：
  - 数值范围
  - 空值处理
  - 与翻译引擎联动的必填校验

#### 测试

- 规则分段单元测试
- LLM 分段返回文本不一致的回退测试
- 词级时间存在时的时间回填测试
- 词级时间缺失时的近似分配测试
- `translator.engine = off + segmentation enabled` 闭环测试
- `translator.engine = google_web` 翻译闭环测试
- `segmentation = auto + no llm config` 自动回退测试
- 双语输出基于分段后原字幕的时间轴一致性测试

### 14.8 建议实施顺序

建议按以下顺序增量实现：

#### 第一步：打通最小后端链路

- 配置模型新增 `segmentation.*`
- 实现规则分段器
- 在现有任务流水线中接入“转写后规则分段”
- 验证“不翻译但可分段”的闭环

#### 第二步：补齐时间对齐策略

- 梳理当前 `whisper.cpp` 输出中可获取的时间粒度
- 优先接入词级时间映射
- 无词级时间时实现近似时间分配
- 在任务详情中记录时间策略来源

#### 第三步：接入 LLM 分段

- 复用现有 LLM 适配层
- 设计只返回断句边界的 prompt
- 做文本一致性校验和失败回退
- 完成 `auto / LLM 优先` 两条策略

#### 第四步：接入 Google 翻译适配器

- 定义独立 `GoogleWebTranslator`
- 接入翻译引擎切换
- 补充实验性提示、限流和错误包装
- 保证不与 LLM 参数耦合

#### 第五步：前端和测试收口

- 配置页交互完善
- 任务摘要信息展示
- 增补回归测试和验收用例

### 14.9 风险与边界补充

#### A. `whisper.cpp` 时间粒度风险

- 当前 CLI 输出若不稳定提供词级时间，则“词级时间优先”可能只能在部分环境下生效
- 应对策略：
  - 明确能力探测
  - 可用则启用，不可用则降级
  - 不把“逐字级对齐”写成首版承诺

#### B. LLM 分段改写原文风险

- LLM 可能在断句时顺手改写、纠错或删字
- 应对策略：
  - prompt 限制
  - 严格文本一致性校验
  - 自动回退规则分段

#### C. Google 适配器稳定性风险

- 接口可能变更、限流、验证码或区域不可用
- 应对策略：
  - 标记实验性
  - 单独适配器隔离
  - 不作为首版稳定承诺

#### D. 分段后观感风险

- 即使完成规则分段和近似时间分配，仍可能存在“观感上略差一点”的片段
- 应对策略：
  - 将“观感基本对齐”作为首版目标
  - 将独立 `forced alignment` 保留到后续版本

### 14.10 本节交付定义

当以下条件满足时，可视为 `0001` 新增需求已完成对应实现：

- 配置中可独立选择 `翻译引擎` 与 `原字幕分段策略`
- 不翻译时仍可对原字幕做智能分段
- `自动` 分段在无 LLM 配置时可自动回退到规则分段
- `Google Web 免费模式（实验性）` 可作为独立翻译适配器接入，不参与分段
- 翻译输入基于“分段后的原字幕”
- 时间轴优先使用词级时间，缺失时可降级为近似对齐
- 任务快照、任务状态、配置页和测试计划均覆盖新增字段与流程
